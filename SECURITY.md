# Security

This plugin handles Google OAuth tokens. If you find a vulnerability:

- Open a private security advisory on GitHub, or an issue with no exploit details.
- Never post tokens, refresh tokens, or account files in issues.

Token files live outside this repo (your Hermes profile directory) and are never committed here. The `.gitignore` excludes all bytecode and caches; keep it that way.
